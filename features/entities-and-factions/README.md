# Entities and factions

Owns characters, organizations, factions, relationships, reputation, attributes, and entity lifecycle rules.

Changes must document identity fields, relationship effects, state transitions, serialization, and UI/API consumers.
