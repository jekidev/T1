# Game core

Owns the simulation loop, turn progression, global rules, balancing, and shared game-state contracts.

Implementation references should point to the relevant files under `artifacts/command-sim`, API schemas, and backend routes. New mechanics must document state changes, side effects, save compatibility, and acceptance criteria before integration.
