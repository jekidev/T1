# Deployment and runtime

Owns setup, Replit, Manus, Termux, GitHub Pages, environment validation, process startup, and runtime health checks.

Implementation references include `deployment/`, `scripts/setup.mjs`, `.replit`, and GitHub Actions workflows.
